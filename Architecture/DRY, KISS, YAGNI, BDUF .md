

Конспект — DRY, KISS, YAGNI, BDUF

1. DRY — Don’t Repeat Yourself (Не повторяйся)

Суть:
Каждый фрагмент знаний/логики в программе должен иметь единственное место реализации.

Зачем:
	•	Меньше дублирования → проще поддержка.
	•	Исправляешь баг или меняешь логику — правишь только в одном месте.

Пример нарушения:

// Плохо — одна и та же логика в двух местах
function calcPriceWithTax(price) {
  return price + price * 0.2;
}
function calcDiscountPrice(price) {
  return (price - price * 0.1) + (price - price * 0.1) * 0.2;
}

Исправление:

function addTax(price) {
  return price + price * 0.2;
}
function calcDiscountPrice(price) {
  return addTax(price - price * 0.1);
}


⸻

2. KISS — Keep It Simple, Stupid (Делай проще)

Суть:
Не усложняй код, если можно сделать проще, без лишних абстракций и хитростей.

Зачем:
	•	Проще читать, тестировать и менять код.
	•	Меньше багов из-за ненужной сложности.

Пример нарушения:

// Плохо — слишком сложно для простой задачи
const sum = (...args) => args.reduce((a, b) => a + b, 0);

Для начинающего проще так:

function sum(a, b) {
  return a + b;
}


⸻

3. YAGNI — You Aren’t Gonna Need It (Тебе это не понадобится)

Суть:
Не реализовывай функционал «на будущее», пока он реально не нужен.

Зачем:
	•	Не тратишь время на лишнее.
	•	Не увеличиваешь кодовую базу тем, что может никогда не пригодиться.

Пример нарушения:

// Плохо — заранее делаем поддержку 10 валют, хотя нужна только 1
function convertCurrency(amount, from, to) { ... }

Лучше:
Сделай только то, что требуется сейчас. Если появится задача — добавишь.

⸻

4. BDUF — Big Design Up Front (Большое проектирование заранее)

Суть:
Подход, когда сначала полностью проектируют систему, архитектуру, схемы, документацию, а потом начинают кодить.

Плюсы:
	•	Есть чёткая архитектура, меньше хаоса.
	•	Можно заранее оценить риски и ресурсы.

Минусы:
	•	Потеря гибкости: пока проектируешь, требования могут измениться.
	•	Риск, что часть работы окажется ненужной.

Современный баланс:
	•	В старых методологиях (водопад) BDUF был нормой.
	•	В Agile / Scrum — делают «достаточное проектирование» (Enough Design Up Front), чтобы не тормозить развитие.

⸻

Краткая таблица

Принцип	Что значит	Главная цель
DRY	Не дублировать код	Меньше багов и затрат на поддержку
KISS	Делать проще	Легко читать и менять
YAGNI	Не писать лишнее	Не тратить время впустую
BDUF	Проектировать заранее	Структура и порядок, но без перегиба


⸻

📌 Мой совет: DRY, KISS и YAGNI — применять всегда. BDUF — осторожно, и только в нужной мере, чтобы не убить гибкость разработки.

⸻

Если хочешь, я могу к этому конспекту добавить чек-лист для Pull Request, чтобы можно было перед каждым мержем проверить, что код соответствует DRY / KISS / YAGNI. Это как раз будет практическое применение этих принципов.

Сделать?
# Конспект: I — Interface Segregation Principle (Принцип разделения интерфейсов)

## 1. Определение
- **Суть:** Клиенты не должны зависеть от интерфейсов, которые они не используют.  
- Лучше много маленьких интерфейсов, чем один «толстый».  
- Этот принцип защищает от избыточных обязанностей и пустых реализаций.

## 2. Интерфейс и Клиент
- **Интерфейс** — это набор публичных методов/свойств, которые объект предоставляет внешнему миру. Это *контракт*: «вот что я умею».  
- **Клиент** — это любой код, который этим интерфейсом пользуется. Клиенту всё равно, как методы реализованы внутри, ему важно только, что их можно вызвать.  

📦 Пример:  
```python
class ITimer:
    def start(self): ...
    def stop(self): ...
```
- `ITimer` — это интерфейс (контракт).  
- Код контроллера, который вызывает `timer.start()`, — это клиент.

## 3. Нарушение ISP (❌)
```python
class ITimer:
    def start(self): ...
    def stop(self): ...
    def reset(self): ...
    def export_results(self): ...
    def upload_to_server(self): ...
```

- Все реализации таймера обязаны поддерживать методы `reset`, `export_results`, `upload_to_server`, даже если они не нужны.  
- Это приводит к заглушкам (`raise NotImplementedError`) или ненужной логике.  

## 4. Соблюдение ISP (✅)
Разделяем интерфейсы на маленькие:
```python
class ITimer:
    def start(self): ...
    def stop(self): ...

class Resettable:
    def reset(self): ...

class Exportable:
    def export_results(self): ...

class Uploadable:
    def upload_to_server(self): ...
```

- Базовый таймер реализует только `ITimer`.  
- Если нужен сброс — реализуется `ResettableTimer`.  
- Если нужен экспорт — `ExportableTimer`.  

## 5. Практика на SpeedTypingTest
- **Нарушение:** таймер знает и про время, и про экспорт в БД, и про UI — слишком много обязанностей.  
- **Исправление:**  
  - Интерфейс таймера (`start`, `stop`, `on_done`).  
  - Отдельные интерфейсы для дополнительных функций (экспорт, сброс).  
  - Контроллер использует только то, что ему нужно.  

## 6. Чеклист для применения ISP
- Интерфейс содержит >3–5 методов? → скорее всего, его нужно разделить.  
- Реализация вынуждена писать «заглушки» (`raise NotImplementedError`)? → интерфейс раздут.  
- Разные клиенты используют разные подмножества методов? → раздели интерфейсы.  

## 7. Главное, что вынести
- Интерфейс = контракт («что объект умеет»).  
- Клиент = тот, кто этот контракт использует.  
- Принцип ISP говорит: не заставляй клиентов зависеть от лишних методов.  
- Маленькие интерфейсы → простые реализации → меньше багов.  

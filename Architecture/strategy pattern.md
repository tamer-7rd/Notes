
# Конспект: Паттерн Strategy (Стратегия)

---

## 1. Идея Strategy в одном образе

Представь: у тебя есть задача «добраться до школы». Варианты:
- Пешком,
- На автобусе,
- На велосипеде.

Цель всегда одна — добраться, но способов несколько.  
**Strategy — это когда мы делаем набор способов и выбираем нужный в зависимости от ситуации.**

---

## 2. Где мы встречаем Strategy в жизни
- Магазин предлагает разные способы оплаты: картой, Apple Pay, криптой.
- В поиске можно включить «точный поиск» или «умный поиск с подсказками».
- В телефоне можно разблокировать: по паролю, по отпечатку, по лицу.

Все это — стратегии: одна задача, много решений.

---

## 3. Зачем Strategy программисту

Если написать всё через if/else, код будет разрастаться:

```python
if method == "password":
    ...
elif method == "google":
    ...
elif method == "github":
    ...
```

### Проблемы:
- С каждым новым методом код всё тяжелее читать.
- Такие «лестницы условий» повторяются в разных местах.
- Трудно тестировать каждую часть отдельно.

**Strategy решает это:** каждый способ в отдельной «коробке», а главный код просто выбирает коробку.

---

## 4. Схема Strategy
1. Общая задача — «сортировка», «авторизация», «поиск».
2. Набор стратегий — каждый способ реализован отдельно.
3. Контекст — место, где выбирается стратегия (конфиг, флаг, пользовательский выбор).

---

## 5. Когда Strategy нужен
- Вариантов три и больше.
- Алгоритмы действительно разные, а не отличаются только параметрами.
- Нужно выбирать во время работы программы, а не заранее.
- Хочешь легко добавлять новые варианты, не переписывая старый код.
- Один и тот же выбор нужен в разных местах.

---

## 6. Когда хватит if/else
- Вариантов всего 1–2, и новые точно не появятся.
- Отличие только в параметрах (например, лимит = 10 или 20).
- Код вызывается в одном месте и не дублируется.

---

## 7. Примеры Strategy в коде

### Сортировка

```python
def sort_asc(data): return sorted(data)
def sort_desc(data): return sorted(data, reverse=True)
def sort_random(data): 
    import random; res = data[:]; random.shuffle(res); return res

strategies = {"asc": sort_asc, "desc": sort_desc, "random": sort_random}

def sort_data(data, mode="asc"):
    return strategies[mode](data)

print(sort_data([5,3,1], "asc"))
print(sort_data([5,3,1], "desc"))
print(sort_data([5,3,1], "random"))
```

### Авторизация

```python
def login_password(user, pwd): return f"{user} вошёл по паролю"
def login_google(token): return f"Вход через Google: {token}"
def login_github(token): return f"Вход через GitHub: {token}"

auth_strategies = {"password": login_password, "google": login_google, "github": login_github}

def authenticate(method, *args):
    return auth_strategies[method](*args)

print(authenticate("password", "Tamerlan", "1234"))
print(authenticate("google", "GOOGLE_TOKEN"))
```

---

## 8. Где Strategy «выстреливает» особенно ярко
- Фронтенд: выбор алгоритма поиска, разные варианты сортировки списка, разные UI-валидации.
- Бэкенд: разные алгоритмы кэширования (LRU, LFU), разные способы авторизации, разные политики логирования.
- Data Science: выбор модели (линейная регрессия, дерево решений, нейросеть) по настройке.

---

## 9. Главная мысль

Strategy — это не про красивые классы, а про то, чтобы твой код умел легко переключаться между вариантами и не превращался в кашу из условий.

---

📌 **Запомни формулу:**
- Два варианта = if/else.
- Три и больше, плюс перспектива новых — Strategy.
- Отличия только в цифрах → параметры, а не стратегии.

---

## Когда Strategy — это «в самый раз»
1. Вариантов ≥ 3 и они живут своей жизнью.  
   Разные способы сделать одно и то же (поиск, сортировка, авторизация, ценообразование), и ты реально переключаешь их по ситуации/настройке.
2. Выбор делается во время работы.  
   Зависит от объёма данных, фичефлага, профиля пользователя, окружения (dev/prod), загруженности сервиса, региона, AB-теста.
3. Алгоритмы будут меняться/добавляться.  
   Хочешь «подкладывать» новые реализации без перепила всего кода. Нужно масштабировать без каскада правок.
4. Одна и та же развилка расползлась по проекту.  
   Такие же if method == ... уже встречаются в нескольких местах. Это сигнал «вынеси в одну точку выбора + набор стратегий».
5. Нужно изолированно тестировать.  
   Хочешь писать отдельные юнит-тесты на каждый алгоритм без запуска всего приложения.
6. Нужна «настраиваемость без релиза».  
   Хочешь переключать алгоритм конфигом/переменной окружения/хедером запроса.

---

## Когда if/else честно побеждает
1. Вариантов 1–2, простые и вряд ли вырастут.  
2. Выбор никогда не делается во время работы (решается один раз при запуске).  
3. Поведение отличается только параметрами, а не логикой (значит, прокинь параметры, а не множь стратегии).  
4. Код используется в одном месте и не размножается.

---

## Правило мизинца

Если можешь назвать алгоритмы отдельными именами («fuzzy-поиск», «поиск подстрокой», «перцентиль-95», «JWT-логин»), и предполагаешь третий-четвёртый вариант — готовь Strategy.

---

## Практический чек-лист

- Есть минимум три альтернативы? Да → к Strategy.  
- Нужен runtime-переключатель (флаг/хедер/размер данных)? Да → к Strategy.  
- Одна и та же развилка встречается в разных модулях? Да → к Strategy.  
- Хочешь A/B-тесты/фичефлаги без релиза? Да → к Strategy.  
- Отличия только в числах/порогах? Да → пока if/else + параметры.

---

## Мини-пример: как выглядит эволюция

### Было (нормально на старте)
```python
def search(query, items, mode):
    if mode == "fast":
        return [x for x in items if query.lower() in x.lower()]
    elif mode == "smart":
        return fuzzy_search(items, query)  # другая логика
    elif mode == "trigram":
        return trigram_search(items, query)
    else:
        return []
```

**Проблемы начнутся, когда:**
- появится четвёртый/пятый режим,
- эти ветки понадобятся ещё в двух-трёх местах,
- захочешь переключать режим заголовком X-Search-Mode или конфигом.

### Стало (Strategy с реестром)
```python
from typing import Callable, List, Dict

SearchFn = Callable[[str, List[str]], List[str]]

def search_fast(q, xs): return [x for x in xs if q.lower() in x.lower()]
def search_smart(q, xs): ...
def search_trigram(q, xs): ...

REGISTRY: Dict[str, SearchFn] = {
    "fast": search_fast,
    "smart": search_smart,
    "trigram": search_trigram,
}

def search(query: str, items: List[str], mode: str) -> List[str]:
    fn = REGISTRY.get(mode, search_fast)
    return fn(query, items)
```

Теперь:
- добавляешь новый алгоритм → дописал функцию и одну строку в REGISTRY;
- выбор можно делать где угодно: по хедеру, флагу, эвристике объёма данных.

---

## Где Strategy «стреляет» особенно хорошо
- Авторизация/платежи/доставки: разные провайдеры — у каждого своя логика.
- Поиск/ранжирование/сортировка: быстрый vs качественный, локаль-специфичные правила.
- Агрегации метрик: среднее/медиана/перцентили, разные окна времени.
- Ограничение скорости (rate limit): токен-бакет, скользящее окно, leaky bucket.
- Кеш-политики: LRU/LFU/FIFO.
- Сжатие/сериализация: gzip, brotli, zstd; json/msgpack.

---

## Частые ошибки
- Стратегии, которые отличаются только числами. Это не стратегии — это конфиг.  
- Гигантская фабрика ради двух веток. Преждевременная абстракция замедляет.  
- Непрозрачный выбор. Спрятали селектор слишком глубоко → команда не понимает «почему выбралась именно эта стратегия». Делай выбор явно и наблюдаемо (логируй).

---

## Правило здравого смысла
- Начинай с простого if/else.  
- Как только увидел третий алгоритм или второе место в коде с той же развилкой — выноси в Strategy.  
- Переключение делай данными (конфиг/флаг/хедер), а не пересборкой кода.

# Конспект: API, REST API и API-слой

---

## 1. Что такое API

**API (Application Programming Interface)** = «Программный интерфейс».  
Набор правил, по которым программы или части одной программы общаются между собой.

**Примеры:**
- Запрос к погодному сервису → это их API (он отдаёт JSON).
- Python-библиотека `os` даёт функции для работы с файлами → это тоже API.
- Браузер и Flask-приложение общаются через HTTP-запросы → это веб-API.

👉 **API = язык общения программ, а не людей.**

---

## 2. Что такое REST API

**REST (Representational State Transfer)** = стиль построения веб-API, набор правил.

### Основные правила REST:
1. **HTTP-методы по назначению**
   - GET → получить данные  
   - POST → создать данные  
   - PUT/PATCH → обновить данные  
   - DELETE → удалить данные  

2. **Ресурсные URL**
   - `/api/users` → список пользователей  
   - `/api/users/5` → конкретный пользователь  

3. **Ответы в формате JSON (а не HTML)**
```json
{"id":5,"name":"Tamerlan"}
```

4. **Правильные статус-коды**
   - 200 OK — всё хорошо  
   - 201 Created — ресурс создан  
   - 400 Bad Request — ошибка клиента  
   - 404 Not Found — не найдено  
   - 500 Internal Server Error — ошибка сервера  

5. **Stateless**: каждый запрос самодостаточный (не зависит от прошлых).

---

## 3. JSON и его роль

- JSON = текстовый формат для обмена данными.  
- Похож на объект JS, но это строка.  
- Пример:
```json
{"user":"Tamerlan","wpm":92,"accuracy":0.97}
```

- В браузере → `response.json()` или `JSON.parse`.  
- В Python → `json.loads`.  

👉 **JSON = мост между сервером (Python dict) и клиентом (JS объект).**

---

## 4. Что такое API-слой

**API-слой** = часть приложения, которая отвечает за обмен данными фронта и бэка.

- На фронте (React, JS): отдельные файлы с функциями для запросов (axios/fetch).  
```js
export const getUsers = () => axios.get("/api/users");
```
- На бэке (Flask/Node): маршруты `/api/...`, которые возвращают JSON.  
Обычно:  
  - Controller → принимает запрос  
  - Service → бизнес-логика  
  - Repository → база данных  

👉 В проекте: `/contact` = HTML-страница, `/api/contact` = JSON-эндпоинт.

---

## 5. Когда нужен REST/API-слой

REST **не обязателен**, но нужен если:  
- нужен «живой» интерфейс (AJAX),  
- планируется фронт на React/Vue/Next,  
- будет мобильное приложение или боты,  
- нужна «чистая» архитектура (данные отдельно).  

👉 Можно гибрид: оставить HTML-страницы, а JSON-эндпоинты добавить точечно.

---

## 6. Как спрашивают на собесе

**Вопросы:**
- Что такое API?  
- Чем REST API отличается от API?  
- Почему важны HTTP-методы?  
- Чем HTML-страница отличается от JSON-ответа?  
- Что такое API-слой?  

**Пример ответа:**  
API — это способ общения программ. REST API — это API по правилам REST: методы, URL, JSON, статус-коды. В проекте есть API-слой: он отвечает за обмен фронта и бэка. На фронте — функции axios, на бэке — эндпоинты Flask, которые возвращают JSON. HTML-маршруты нужны человеку, API-маршруты — программе.

---

📌 **Главное:**
- API = общий язык программ.  
- REST API = API по единым правилам.  
- API-слой = место в коде, где фронт и бэк обмениваются JSON.

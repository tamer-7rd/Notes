# HashMap / HashSet — Конспект

---

## 🔑 Что такое HashMap и HashSet

Обе структуры строятся на **хеш-таблице** (массив + хеш-функция, которая быстро вычисляет, куда класть/искать элемент).

- **HashMap** (или `dict` в Python, `Map` в JS) — хранит пары ключ → значение.  
- **HashSet** (или `set` в Python, `Set` в JS) — хранит только уникальные значения, без дубликатов.  

---

## ⚡ HashMap (словарь)

📖 **Определение**: структура данных, где каждый ключ уникален, а к нему привязано значение.  

👉 Python:
```python
scores = {"Alice": 90, "Bob": 85}
print(scores["Alice"])  # 90
```

👉 JavaScript:
```js
let scores = new Map();
scores.set("Alice", 90);
scores.set("Bob", 85);
console.log(scores.get("Alice")); // 90
```

- **Операции**: поиск, вставка, удаление → в среднем `O(1)` (очень быстро).  
- **Когда использовать**: нужен быстрый доступ по ключу (например, «дай мне счёт игрока по имени»).  

---

## ⚡ HashSet (множество)

📖 **Определение**: структура, которая хранит только уникальные элементы, без привязки к значениям.  

👉 Python:
```python
visited = {1, 2, 3}
visited.add(2)  # не добавится второй раз
print(visited)  # {1, 2, 3}
```

👉 JavaScript:
```js
let visited = new Set([1, 2, 3]);
visited.add(2); 
console.log(visited); // Set {1, 2, 3}
```

- **Операции**: добавление, проверка «есть ли элемент?» → `O(1)` в среднем.  
- **Когда использовать**: если нужно хранить только уникальные значения (например, ID пользователей).  

---

## 🧩 Задачи подсчёта (Counting Problems)

Именно тут HashMap/HashSet становятся «оружием джедая».  

### 1. Подсчёт частоты слов (HashMap)

Вопрос: «Сколько раз каждое слово встречается в тексте?»  

👉 Python:
```python
text = "cat dog cat fish dog cat"
counts = {}
for word in text.split():
    counts[word] = counts.get(word, 0) + 1
print(counts)  # {'cat': 3, 'dog': 2, 'fish': 1}
```

👉 JS:
```js
let text = "cat dog cat fish dog cat";
let counts = new Map();
for (let word of text.split(" ")) {
  counts.set(word, (counts.get(word) || 0) + 1);
}
console.log(counts); // Map { 'cat' => 3, 'dog' => 2, 'fish' => 1 }
```

---

### 2. Проверка уникальности (HashSet)

Вопрос: «Все ли элементы массива уникальны?»  

👉 Python:
```python
arr = [1, 2, 3, 2]
print(len(arr) == len(set(arr)))  # False
```

👉 JS:
```js
let arr = [1, 2, 3, 2];
console.log(arr.length === new Set(arr).size); // false
```

---

### 3. Первое уникальное число (HashMap + порядок)

👉 Задача: найти первое число в массиве, которое встречается 1 раз.  

```python
arr = [4, 5, 1, 2, 1, 2, 5]
counts = {}
for x in arr:
    counts[x] = counts.get(x, 0) + 1

for x in arr:
    if counts[x] == 1:
        print(x)  # 4
        break
```

---

## 🎯 Как это связано с твоим уровнем

Ты уже знаешь массивы и циклы.  
- В **массиве** поиск элемента = `O(n)` (надо пройти весь массив).  
- В **HashMap/HashSet** поиск = `O(1)` (моментально).  

👉 Это решает массу задач: подсчёт очков, проверка уникальности логина, хранение состояния и т.д.  

---

## 🔥 Упражнение для тебя

Напиши на **Python или JS** функцию, которая принимает строку и возвращает **первое слово, которое встречается дважды**.  

Пример:  
```
"dog cat fish dog cat" → "dog"
```

## Решение: 

def first_duplicate_word(line):
    seen = set()                 # 1) Пустое множество (HashSet)
    for word in line.split():    # 2) Идём слева направо по словам
        if word in seen:         # 3) Проверка членства в set = O(1) в среднем
            return word          # 4) Первое слово, которое уже встречалось
        seen.add(word)           # 5) Запоминаем слово
    return None




# 🧩 Конспект: Хеш, Хеш-таблица и Корзины  

---

## 🔑 Что такое хеш
- **Хеш** — это число, вычисленное из данных с помощью хеш-функции.  
- Одно и то же значение → один и тот же хеш (в рамках одного запуска).  
- Разные значения → обычно разные хеши, но могут совпасть (**коллизия**).  

👉 Пример:  
`"dog"` → `5478754569593525716`  
`"cat"` → `-7318543139716710243`  

---

## 🗂 Что такое хеш-таблица
- В основе HashMap/HashSet лежит **большой массив**, разделённый на ячейки.  
- Эти ячейки называются **корзины (buckets)**.  
- Чтобы положить или найти элемент:  
  1. Считаем хеш.  
  2. Берём остаток от деления на размер массива → получаем номер корзины.  
  3. Идём в эту корзину.  
  4. Если там несколько элементов — сравниваем, чтобы найти точное совпадение.  

---

## 🧺 Корзина
- Корзина = ячейка массива, куда попали все элементы с одинаковым остатком от деления.  
- Если хеши совпали → элементы делят одну корзину (**коллизия**).  
- Внутри корзины хранится небольшой список или дерево.  

---

## ⚡ Временная сложность
- **В среднем (амортизированно):**  
  - вставка, поиск, удаление = `O(1)`  
  - потому что элементы равномерно распределяются, и в корзине обычно мало элементов (0 или 1).  

- **В худшем случае:**  
  - все элементы попали в одну корзину → линейный поиск → `O(n)`.  

- Чтобы этого избежать, используют:  
  - **хорошие хеш-функции**,  
  - **увеличение массива и перераспределение (ресайзинг)** при переполнении.  

---

## 🎯 Разница между HashMap и HashSet
- **HashMap** = хеш-таблица ключ→значение.  
- **HashSet** = та же таблица, но хранятся только ключи (значение не нужно).  

---

## 📌 Итог
- HashMap/HashSet быстры, потому что используют **хеширование** → доступ к элементу напрямую по адресу корзины.  
- Корзины решают проблему коллизий.  
- В среднем — это «моментально» (O(1)), в худшем случае — линейный поиск (O(n)), но случается редко.  
# 📖 Конспект: Big O и массивы

## 1. Что такое Big O

-   **Big O notation** = математическая запись для описания
    эффективности алгоритмов.
-   **O** = Order («порядок роста»).
-   **n** = количество данных (размер массива, число элементов).
-   **O(f(n))** = «как растёт количество шагов алгоритма в зависимости
    от размера входа».

⚡ Важно: Big O ≠ миллисекунды → мы измеряем рост шагов, а не конкретное
время.

------------------------------------------------------------------------

## 2. Основные сложности, которые нужно знать до 01.01.2026

-   **O(1)** --- константная.\
    → доступ к элементу массива по индексу.\
-   **O(log n)** --- логарифмическая.\
    → бинарный поиск (делим пополам).\
-   **O(n)** --- линейная.\
    → пройти весь массив.\
-   **O(n log n)** --- почти линейная.\
    → эффективные сортировки (quicksort, mergesort).\
-   **O(n²)** --- квадратичная.\
    → вложенные циклы, пузырьковая сортировка.\
-   **O(2\^n), O(n!)** --- экспоненциальные, факториальные (редко
    используются, очень медленные).

👉 В продакшене чаще всего встречаются O(1), O(log n), O(n), O(n log
n).\
O(n²) знать важно, чтобы понимать, почему вложенные циклы опасны.

------------------------------------------------------------------------

## 3. Как читать логарифм

-   **log₂(n)** = «сколько раз нужно разделить n на 2, чтобы получить
    1».\
-   Пример:
    -   log₂(8) = 3 → 8 → 4 → 2 → 1.\
    -   log₂(16) = 4.\
-   В алгоритмах почти всегда log₂, но пишут просто log n.\
-   Главное: логарифм растёт очень медленно (для 1 000 000 всего ≈ 20
    шагов).

------------------------------------------------------------------------

## 4. Как анализировать алгоритм

-   Один цикл по n → O(n).\
-   Два вложенных цикла по n → O(n²).\
-   Каждый раз делим пополам → O(log n).\
-   Делим пополам + обрабатываем все элементы → O(n log n).\
-   Прямой доступ к элементу или хэш-таблица → O(1).

Правила:\
- Последовательные шаги → складываем (O(n) + O(log n) → O(n)).\
- Вложенные циклы → умножаем.\
- Берём только старший член, константы игнорируем.

------------------------------------------------------------------------

## 5. Массивы: операции и их сложность

-   **Доступ по индексу** → O(1).\
-   **Поиск**:
    -   Линейный (неотсортированный массив) → O(n).\
    -   Бинарный (отсортированный массив) → O(log n).\
-   **Вставка**:
    -   В конец → O(1) (амортизированно).\
    -   В начало или середину → O(n) (сдвиг элементов).\
-   **Удаление**:
    -   С конца → O(1).\
    -   С начала или середины → O(n) (сдвиг элементов).

------------------------------------------------------------------------

## 6. Разница O(n log n) и O(n²)

-   **O(n log n)** = «умные алгоритмы», например сортировки.
    -   Делим данные пополам (log n уровней).\
    -   На каждом уровне обрабатываем все n элементов.\
    -   Итог: n \* log n шагов.\
-   **O(n²)** = «наивные алгоритмы», вложенные циклы.
    -   Для каждого элемента проходим весь массив.\
    -   Итог: n \* n шагов.

Пример:\
- n = 1 000 000 →\
- O(n log n) ≈ 20 млн шагов.\
- O(n²) = 1 триллион шагов → слишком долго.

------------------------------------------------------------------------

## 7. Зачем это джуну

-   В продакшене ты постоянно работаешь с массивами, коллекциями,
    сортировками.\
-   Даже если сам не пишешь «алгоритмы сортировки», нужно уметь заметить
    неэффективность (например, вложенный цикл на большом массиве).\
-   На собеседовании джуна тебя обязательно спросят:
    -   «Какая сложность поиска, вставки, удаления в массиве?»\
    -   «Что значит O(n)?»

------------------------------------------------------------------------

## 8. Мини-примеры на коде (JS)

### Линейный поиск --- O(n)

``` js
function find(arr, target) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] === target) return i;
  }
  return -1;
}
```

### Бинарный поиск --- O(log n)

``` js
function binarySearch(arr, target) {
  let left = 0, right = arr.length - 1;
  while (left <= right) {
    let mid = Math.floor((left + right) / 2);
    if (arr[mid] === target) return mid;
    if (arr[mid] < target) left = mid + 1;
    else right = mid - 1;
  }
  return -1;
}
```

### Вложенные циклы (O(n²))

``` js
function hasDuplicates(arr) {
  for (let i = 0; i < arr.length; i++) {
    for (let j = i + 1; j < arr.length; j++) {
      if (arr[i] === arr[j]) return true;
    }
  }
  return false;
}
```

------------------------------------------------------------------------

## 9. Что ты должен уметь сказать (своими словами)

-   O(1) → константное время, доступ по индексу.\
-   O(n) → линейное время, поиск в массиве.\
-   O(log n) → бинарный поиск, деление пополам.\
-   O(n log n) → эффективная сортировка.\
-   O(n²) → вложенные циклы, пузырьковая сортировка.

------------------------------------------------------------------------

⚡ **Итого**:\
До 01.01.2026 тебе нужно:\
- Понимать определение Big O.\
- Знать 5 ключевых сложностей: O(1), O(log n), O(n), O(n log n), O(n²).\
- Уметь объяснить простыми словами с примерами из массивов.\
- Видеть это в коде: цикл → O(n), вложенный цикл → O(n²), деление
пополам → O(log n).

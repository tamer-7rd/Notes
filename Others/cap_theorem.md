# CAP-теорема и базы данных

## 0. Что такое распределенные системы?

**Распределенная система (Distributed System)** - это система, состоящая из нескольких компьютеров (узлов/nodes), которые работают вместе как единое целое, но физически находятся в разных местах и связаны через сеть.

### Простые примеры:
- **Банкоматы (ATMs)** - тысячи устройств по всему миру, но все работают с одной банковской системой
- **Социальные сети (Social Networks)** - миллионы серверов в разных дата-центрах (data centers), но пользователи видят единую платформу
- **Облачные сервисы (Cloud Services)** - Google, Amazon используют тысячи серверов одновременно

### Зачем нужны распределенные системы?
1. **Масштабируемость (Scalability)** - можно добавлять новые серверы при росте нагрузки
2. **Надежность (Reliability)** - если один сервер упал, остальные продолжают работать
3. **Географическое распределение (Geographic Distribution)** - серверы ближе к пользователям = быстрее ответ

### Проблема распределенных систем:
Когда серверы находятся в разных местах и связаны сетью, **сеть может "порваться"** - это называется **сетевое разделение (Network Partition)**. 

Именно в этот момент **CAP-теорема (CAP Theorem)** говорит: "Ребята, вы не можете иметь все сразу - выбирайте что важнее!"

------------------------------------------------------------------------

## 0.1. При чем тут базы данных?

### Проблема: одна база данных не справляется

Представьте, что у вас есть интернет-магазин с **одной базой данных (Single Database)** на одном сервере:
- 1000 пользователей одновременно → сервер тормозит
- 10,000 пользователей → сервер падает
- Сервер сломался → весь сайт не работает

### Решение: распределенная база данных

Вместо одной базы данных создают **распределенную систему баз данных (Distributed Database System)**:

```
Пользователь в Москве → Сервер в Москве → База данных в Москве
Пользователь в Нью-Йорке → Сервер в Нью-Йорке → База данных в Нью-Йорке
Пользователь в Токио → Сервер в Токио → База данных в Токио
```

### Но возникает проблема CAP!

Теперь у вас **несколько баз данных (Multiple Databases)**, которые должны работать как одна система. И тут появляются вопросы:

1. **Consistency (Согласованность)**: Если пользователь в Москве изменил профиль, когда пользователь в Токио увидит эти изменения?

2. **Availability (Доступность)**: Если связь между Москвой и Токио прервалась, должны ли базы данных продолжать работать?

3. **Partition Tolerance (Устойчивость к разделению)**: Что делать, если сеть "порвалась" между дата-центрами (data centers)?

### Примеры распределенных баз данных (Distributed Databases):

- **Cassandra** - данные хранятся на множестве серверов (multiple servers)
- **MongoDB** - может работать в кластере (cluster) из нескольких машин  
- **Google Spanner** - база данных Google, разбросанная по всему миру
- **Amazon DynamoDB** - облачная база данных (cloud database) Amazon

### Почему это важно?

Современные приложения (Instagram, WhatsApp, Netflix) обслуживают миллиарды пользователей. Одна база данных просто не справится - нужны распределенные системы баз данных (distributed database systems). И CAP-теорема объясняет, какие компромиссы (trade-offs) приходится делать при их проектировании.

------------------------------------------------------------------------

## 1. Суть CAP-теоремы

**CAP-теорема (CAP Theorem)** утверждает: распределённая система **не может одновременно
гарантировать все три свойства (properties)**:

1.  **C --- Consistency (Согласованность)**\
    Все узлы (nodes) видят одни и те же данные в один и тот же момент.

2.  **A --- Availability (Доступность)**\
    Каждый запрос (request) получает ответ (даже если часть узлов недоступна).

3.  **P --- Partition Tolerance (Устойчивость к разделению сети)**\
    Система продолжает работать, даже если сеть «порвалась» (network partition) и узлы не
    могут связаться между собой.

⚡ В реальном мире разделения сети (network partitions) случаются, поэтому приходится
выбирать: либо **CP**, либо **AP**.

------------------------------------------------------------------------

## 2. Примеры категорий

### CP (Consistency + Partition tolerance)

Жертвуют доступностью (sacrifice availability): лучше временно не отвечать, чем показать неверные
данные (inconsistent data).\
📌 Примеры: - **HBase** - **MongoDB (в строгом режиме/strict mode)** - **Google
Spanner**

Сценарий: **банк (banking system)**. Лучше заморозить транзакции (freeze transactions), чем потерять деньги или
задвоить списание (duplicate transactions).

------------------------------------------------------------------------

### AP (Availability + Partition tolerance)

Жертвуют строгой согласованностью (sacrifice strict consistency): система всегда отвечает, но данные
могут быть временно разными на разных узлах (temporarily different on different nodes).\
📌 Примеры: - **Cassandra** - **Amazon DynamoDB** - **CouchDB**

Сценарий: **соцсеть (social network)**. Лента (feed) или лайки (likes) могут обновляться с задержкой (eventual consistency), но
приложение работает.

------------------------------------------------------------------------

### CA (Consistency + Availability)

Возможна только в идеальных условиях (нет сетевых разделений/no network partitions). В
реальных распределённых системах почти недостижимо (nearly impossible).\
📌 Примеры: - **PostgreSQL, MySQL** (на одном сервере/single server).

Сценарий: классическая монолитная база (monolithic database) без разделения сети.

------------------------------------------------------------------------

## 3. Как объяснить на собеседовании

*«CAP-теорема утверждает, что при сетевом разделении (network partition) приходится выбирать
между консистентностью (consistency) и доступностью (availability). Банковские системы (banking systems) чаще выбирают
CP, соцсети (social networks) --- AP. CA встречается только на одиночных серверах (single servers).»*

------------------------------------------------------------------------

## 4. Метафора

Друзья ведут общий список покупок (shared shopping list) в заметках: - **Consistency**: у всех
список одинаковый прямо сейчас (same list right now).\
- **Availability**: каждый всегда может открыть и редактировать список (always can open and edit).\
- **Partition tolerance**: даже без интернета (without internet) список должен работать.

Если интернет пропал (internet is down), то:\
- **CP**: запрещаем редактирование (disable editing), пока не вернётся связь (until connection restored).\
- **AP**: разрешаем редактировать (allow editing), но потом «сшиваем» разные версии (merge different versions later).

------------------------------------------------------------------------

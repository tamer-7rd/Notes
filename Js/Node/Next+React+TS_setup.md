# 🚀 Установка Next.js + React + TypeScript

> **Гайд по установке Next.js + React + TypeScript в уже существующую папку**

---

## 📦 1. Установи зависимости

Перейди в корень проекта и выполни:

```bash
pnpm add next@latest react@latest react-dom@latest
pnpm add -D typescript @types/react @types/node @types/react-dom
```

> 💡 **Результат:** У тебя появятся основные пакеты и TypeScript типы.

---

## ⚙️ 2. Добавь скрипты в package.json

Открой `package.json` и допиши то чего нет (сохранив твои `engines` и `packageManager`):

```json
{
  "name": "ainews",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "typescript": "5.6.2",
    "@types/node": "20.11.30",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0"
  },
  "engines": {
    "node": ">=22.0.0 <23",
    "pnpm": "10.18.0"
  },
  "packageManager": "pnpm@10.18.0"
}
```

---

## 🔧 3. Настрой TypeScript

- ✅ **Если у тебя `tsconfig.json` уже есть** — Next его подхватит автоматически
- 🆕 **Если файла нет** — Next создаст дефолтный при первом запуске (`pnpm dev`)

---

## 📄 4. Создай стартовую страницу

В папке `src/app` добавь файл `page.tsx`:

```tsx
export default function Home() {
  return (
    <main>
      <h1>Hello Next.js + TypeScript 🚀</h1>
    </main>
  )
}
```

---

## 🎯 5. Запусти проект

```bash
pnpm dev
```

🌐 Откроется [http://localhost:3000](http://localhost:3000) с твоей стартовой страницей.

---

## 🎉 Готово!

Теперь у тебя есть полноценный Next.js проект с TypeScript поддержкой!

### 📚 Полезные команды:

- `pnpm dev` - запуск в режиме разработки
- `pnpm build` - сборка для продакшена
- `pnpm start` - запуск продакшен версии
- `pnpm lint` - проверка кода линтером


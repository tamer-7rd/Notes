
# 🚀 Шпаргалка по React (база для новичка)

---

## 1. Что такое React
React — библиотека для создания сайтов и приложений.  
Главная идея: сайт состоит из **компонентов** (кусочков), как LEGO.

---

## 2. Компоненты
Компонент = функция, которая возвращает HTML (JSX).

```tsx
function Hello() {
  return <h1>Привет, мир!</h1>;
}

// использование:
<Hello />
```

---

## 3. Props (свойства компонента)
Props = входные данные для компонента.

```tsx
function Hello({ name }: { name: string }) {
  return <h1>Привет, {name}!</h1>;
}

// использование:
<Hello name="Тамерлан" />
```

---

## 4. State (состояние)
Компонент может хранить своё состояние через `useState`.

```tsx
import { useState } from "react";

function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>Счётчик: {count}</p>
      <button onClick={() => setCount(count + 1)}>+1</button>
    </div>
  );
}
```

---

## 5. Списки
Для списка используем `.map`. Каждому элементу нужен `key`.

```tsx
function List() {
  const items = ["яблоко", "банан", "груша"];

  return (
    <ul>
      {items.map((fruit, i) => (
        <li key={i}>{fruit}</li>
      ))}
    </ul>
  );
}
```

---

## 6. События
Обрабатываем действия пользователя (клик, ввод и т. д.).

```tsx
function Button() {
  function handleClick() {
    alert("Нажали!");
  }

  return <button onClick={handleClick}>Жми</button>;
}
```

---

## 7. Главные идеи React
- Компонент = функция, рисующая кусок страницы.
- Props = входные данные.
- State = «память» компонента, которая меняется.
- События = реакция на действия пользователя.
- Всё строится из маленьких компонентов, как из деталей.

---

✅ Этого хватает для старта. Дальше можно собирать простые приложения: Header, ArticleCard, List и соединять их в один сайт.

---

## 8. useRef (Ссылки)
Используется для двух целей:
1. Доступ к DOM-элементам напрямую (фокус, скролл).
2. Хранение значений, которые **не вызывают перерисовку** при изменении.

```tsx
import { useRef } from "react";

function FocusInput() {
  const inputRef = useRef<HTMLInputElement>(null);

  const focus = () => {
    // Напрямую обращаемся к DOM-элементу
    inputRef.current?.focus();
  };

  return (
    <>
      <input ref={inputRef} />
      <button onClick={focus}>Фокус</button>
    </>
  );
}
```

---

## 9. Context API (Глобальное состояние)
Позволяет передавать данные глубоко вниз без "prop drilling" (передачи через каждый уровень).

**1. Создаем контекст**
```tsx
import { createContext, useContext } from "react";

// Создаем контекст с дефолтным значением
const ThemeContext = createContext("light");
```

**2. Оборачиваем приложение в Provider**
```tsx
function App() {
  return (
    <ThemeContext.Provider value="dark">
      <Toolbar />
    </ThemeContext.Provider>
  );
}
```

**3. Используем в любом компоненте внутри**
```tsx
function Toolbar() {
  const theme = useContext(ThemeContext);
  return <div>Тема: {theme}</div>; // "dark"
}
```

---

## 10. Custom Hooks (Свои хуки)
Если логика повторяется, её выносят в функцию, название которой начинается с `use`.

```tsx
// Хук для отслеживания ширины окна
function useWindowWidth() {
  const [width, setWidth] = useState(window.innerWidth);

  useEffect(() => {
    const handleResize = () => setWidth(window.innerWidth);
    window.addEventListener("resize", handleResize);
    return () => window.removeEventListener("resize", handleResize);
  }, []);

  return width;
}

// Использование в компоненте
function MyComponent() {
  const width = useWindowWidth();
  return <p>Ширина экрана: {width}px</p>;
}
```

---

## 11. Virtual DOM (Теория для собеседований)
**Virtual DOM** — это легкая копия реального DOM в памяти React.

**Как происходит обновление (Reconciliation):**
1. Изменился state или props.
2. React создает **новое** дерево Virtual DOM.
3. Сравнивает его со **старым** деревом (алгоритм Diffing).
4. Вычисляет минимальный набор изменений.
5. Точечно обновляет **реальный DOM** (только то, что изменилось).

*Это быстрее, чем перерисовывать всю страницу браузера.*

---

## 12. Где читать про useEffect и оптимизацию?
Подробный разбор сложных хуков и мемоизации смотри в отдельном файле:
👉 **`react-hooks.md`** (там про `useEffect`, `useMemo`, `useCallback`, `React.memo`).

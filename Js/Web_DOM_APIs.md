

📚 Конспект: JavaScript, Web API и DOM API

1. Что такое JavaScript (ECMAScript)
	•	ECMAScript — стандарт языка программирования.
	•	Ядро языка включает:
	•	Переменные, типы данных.
	•	Массивы, объекты, функции.
	•	Логические конструкции (if, for, while).
	•	Не зависит от браузера — может работать в любых средах (Node.js, Deno, встраиваемые системы).

⸻

2. Что такое Web API
	•	Web API — набор возможностей, которые предоставляет среда выполнения (браузер) JavaScript-коду.
	•	Не является частью языка.
	•	Живёт только там, где его предоставляет окружение (обычно браузер).
	•	Примеры Web API:
	•	DOM API — работа с HTML-документом.
	•	BOM API — работа с окном браузера (alert(), location, history).
	•	Fetch API — сетевые запросы.
	•	Web Storage API — localStorage, sessionStorage.
	•	Web Animations API — анимации через браузер.
	•	Canvas API — рисование.

⸻

3. Что такое DOM API
	•	Часть Web API.
	•	DOM (Document Object Model) — объектная модель HTML-документа.
	•	DOM API — функции и объекты для:
	•	Поиска элементов (querySelector, getElementById).
	•	Изменения содержимого (.textContent, .classList.add()).
	•	Управления структурой (appendChild, remove()).
	•	Обработки событий (addEventListener).

⸻

4. JavaScript vs Web API
	•	JS (ECMAScript) = язык.
	•	Web API = дополнительные возможности от среды (браузера).
	•	В Node.js нет DOM API, но есть свои API (fs, http и т.д.).
	•	Пример:

const el = document.querySelector('h1'); // DOM API
el.textContent = "Привет";                // DOM API
console.log("Hello");                     // Чистый JS



⸻

5. NodeList и DOM
	•	document.querySelectorAll() → возвращает NodeList, а не массив (Array).
	•	NodeList — это коллекция DOM-узлов (часть DOM API).
	•	Можно перебрать .forEach(), но нет методов массивов (map, filter).
	•	Чтобы получить массив:

let arr = Array.from(document.querySelectorAll('button'));



⸻

6. Можно ли работать с Web API и DOM API из других языков
	•	В браузере:
	•	Можно, если язык компилируется в JS или WebAssembly (Python через Brython, Rust/C++ через Emscripten).
	•	Вне браузера:
	•	Можно через управление браузером (Python + Selenium/Playwright).
	•	Язык отправляет команды браузеру, а тот выполняет через свои Web API.

⸻

7. Ключевая мысль

JavaScript — это только язык.
Всё, что связано с HTML, CSS и браузерными функциями, он делает через Web API.
DOM API — это всего лишь один из разделов Web API.


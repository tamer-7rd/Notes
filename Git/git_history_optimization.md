
# Git: Оптимизация репозитория и Чистка Истории Коммитов

## 🎯 Цель
Научиться поддерживать репозиторий чистым, лёгким и профессиональным: избегать мусора в истории, уметь делать squash, чистить от секретов и тяжёлых файлов, уменьшать вес `.git`, и понимать, когда **нужно**, а когда **не обязательно** переписывать историю.

---

## 📌 1. Проверка веса репозитория

```bash
du -sh .git                  # вес истории
git count-objects -vH        # детальная статистика Git-объектов
du -sh .                    # общий вес проекта
du -sh .[^.]* * | sort -h    # какие папки самые тяжёлые
```

**Интерпретация:**
- `.git < 5MB` — история чистая ✅
- `size-pack > 100MB` — в истории мусор 🔴
- `node_modules/.next` занимают основное место — это нормально, они не в истории

---

## 🧹 2. .gitignore — защита от мусора

Добавить (пример для Next.js + PNPM):

```
node_modules/
.next/
.turbo/
.vercel/
coverage/
*.log
.env
.env.*
.DS_Store
Thumbs.db
```

И создать **.env.example**, вместо коммита `.env`.

---

## ✂️ 3. Правка коммитов перед PR (локальная чистка)

### Посмотреть историю:
```bash
git log --oneline --graph --decorate -20
```

### Изменить последний коммит:
```bash
git commit --amend
```

### Склеить N коммитов:
```bash
git rebase -i HEAD~N
# pick → оставить
# s (squash) или f (fixup) → склеить
```

### Быстрый squash через autosquash:
```bash
git commit --fixup <hash>
git rebase -i --autosquash HEAD~N
```

### После ребейза:
```bash
git log --oneline --graph -20
git push --force-with-lease     # аккуратный пуш
```

---

## 🚨 4. Удаление мусора/секретов из истории (filter-repo)

> Использовать, если `.env`, `node_modules`, `.next`, видео, бинарники попали в историю

### Установка:
```bash
brew install git-filter-repo
```

### Удалить файл/путь из ВСЕЙ истории:
```bash
git filter-repo --path .env --invert-paths
git filter-repo --path node_modules --path .next --invert-paths
```

### Удалить все `.zip`/`.mp4` файлы из истории:
```bash
git filter-repo --path-glob '*.mp4' --path-glob '*.zip' --invert-paths
```

### Очистка и пуш:
```bash
git gc --aggressive --prune=now
git push --force --all
git push --force --tags
```
⚠ После этого **обязательно ротация секретов** — старые ключи считаются скомпрометированными.

---

## 🎛 5. Авто-предотвращение мусора (pre-commit hook)

Создать `.git/hooks/pre-commit`:

```bash
#!/usr/bin/env bash

# Блокировка .env
if git diff --cached --name-only | grep -E '^\.env(\.|$)'; then
  echo "❌ Не коммить .env — используй .env.example"; exit 1
fi

# Блокировка файлов > 5МБ
big=$(git diff --cached --name-only | xargs -I{} bash -c 'test -f "{}" && [ $(wc -c < "{}") -gt 5242880 ] && echo "{}"')
if [ -n "$big" ]; then
  echo "❌ Слишком большие файлы (>5MB):"; echo "$big"; exit 1
fi
```
Сделать исполняемым:
```bash
chmod +x .git/hooks/pre-commit
```

---

## 🧠 6. Когда **нужно**, а когда **не обязательно** чистить историю

| Ситуация | Действие |
|----------|---------|
| Много мелких коммитов, **но ветка ещё не в main** | ✅ лёгкий squash перед merge |
| История публичная, **мусора нет** | ❌ не трогать историю — безопаснее |
| В историю попала `.env`, `node_modules`, видео, архивы | 🔥 filter-repo + пересоздание секретов |
| PR мешает ревью из-за "final", "fix", "typo" | ✅ rebase -i, squash, reword |

---

## ✅ Рекомендованный рабочий цикл перед merge

```bash
git fetch --all
git rebase -i HEAD~N        # подчистить историю
git log --oneline -20        # финальная проверка
git push --force-with-lease # аккуратный пуш
```

---

## 🎯 Вывод

- Историей **не прикалываются**, её **обслуживают осознанно**.
- Не чистишь без причины — но **если чистишь, делаешь идеально**.
- **Squash перед merge → читабельная история**
- **filter-repo → хирургия в случае мусора / секрета**
- Архитектурная культура — это не только код, но и история репозитория.

---

Хочешь, могу:
1. ✅ Подготовить готовый `.gitignore` под Next.js + PNPM
2. ✅ Сгенерировать pre-commit + GitHub Actions для автоматической проверки истории и веса
3. ✅ Добавить ASCII схему "жизненный цикл истории ветки" (как шпаргалку)

Напиши — и прикреплю дополнением.

# Git: Оптимизация репозитория и чистка истории коммитов (расширенный конспект)

> Этот конспект — «рабочая инструкция»: от проверки веса и .gitignore до аккуратного squash, переписывания истории, предотвращения утечек секретов и настройки автоматических проверок.

---

## 🔎 0. TL;DR / Быстрый старт

**Проверить вес и понять, где раздуто:**
```bash
du -sh .git                  # вес истории
git count-objects -vH        # детали по объектам/pack
du -sh .                     # общий вес проекта
du -sh .[^.]* * 2>/dev/null | sort -h  # топ тяжёлых папок
```
**Быстро прибраться локально (не трогая историю):**
```bash
rm -rf .next .turbo .vercel/cache coverage *.log
pnpm store prune  # подсушить PNPM store (глобальный)
```
**Лёгкая чистка истории в своей ветке перед PR:**
```bash
git log --oneline --graph -20
git rebase -i HEAD~N         # squash/fixup/reword последних N коммитов
git push --force-with-lease  # публикуем переписанную ветку
```
**Редактировать ВСЮ историю ветки с самого первого коммита:**
```bash
git rebase -i --root
```
**Хирургия, если в историю попал секрет/мусор:**
```bash
brew install git-filter-repo
# удалить пути из всей истории
git filter-repo --path .env --path node_modules --path .next --invert-paths
# заменить секреты на *** в истории
cat > replacements.txt << 'TXT'
password\s*=\s*.+==>password=***
API_KEY\s*[:=]\s*.+==>API_KEY=***
TXT
git filter-repo --replace-text replacements.txt
# упаковать и запушить
git gc --aggressive --prune=now
git push --force --all && git push --force --tags
```
**Во время rebase при конфликтах:**
```bash
git status
# реши конфликт в файлах, затем
git add <исправленные_файлы>
git rebase --continue
# при необходимости: git rebase --abort  или  git rebase --skip
```
**Страховка перед любым rewrite:**
```bash
git branch backup/before-rewrite-$(date +%Y%m%d-%H%M)
# или полноформатный зеркальный бэкап
git clone --mirror . ../backup-repo.mirror
```

---

## 🎯 1. Цель и принципы

- История — не свалка, а инструмент: читать, искать регрессии (bisect), показывать ход мысли.
- Переписывай историю **только там, где это безопасно**: в своих feature-ветках перед PR. Публичные ветки (main) — переписывать **только в случае инцидента** (секреты/мусор).
- Красота без фанатизма: мелкие осмысленные коммиты нормальны. Склеиваем перед merge, чтобы ревью читался как сценарий.

---

## ⚖️ 2. Проверка веса и диагностика

```bash
du -sh .git                  # общий вес истории
git count-objects -vH        # count/size/size-pack/prune-packable/garbage
```
**Как читать `git count-objects -vH`:**
- `size-pack` — упакованные объекты. Если \>100MB — скорее всего в истории были большие файлы.
- `garbage`/`size-garbage` — мусор в объектном хранилище.

```bash
du -sh .                     # общий вес проекта
du -sh .[^.]* * 2>/dev/null | sort -h  # какие папки тяжелее всего
```
Обычно «вес» дают `node_modules`, `.next`, `.turbo`, `public/assets`. Это ок — **это не история**, если в `.gitignore` всё настроено.

**Глубокая диагностика тяжёлых объектов (по SHA):**
```bash
# найти самые тяжёлые объекты в pack
git verify-pack -v .git/objects/pack/*.idx | sort -k3 -n | tail -10
# сопоставить SHA -> путь файла
git rev-list --objects --all | grep <SHA>
```

---

## 🚫 3. .gitignore — защита от мусора

Пример для Next.js + PNPM:
```gitignore
node_modules/
.next/
.turbo/
.vercel/
coverage/
*.log
.env
.env.*
.DS_Store
Thumbs.db
```
Создай `.env.example` (переменные без значений/фиктивные) и никогда не коммить `.env`.

---

## ✂️ 4. Локальная чистка истории перед PR (безопасно)

### 4.1. Посмотреть историю
```bash
git log --oneline --graph --decorate -20
```

### 4.2. Исправить последний коммит
```bash
# добавить файлы в последний коммит
git add <файлы>
git commit --amend --no-edit
# или поменять сообщение
git commit --amend
```

### 4.3. Склеить последние N коммитов (interactive rebase)
```bash
git rebase -i HEAD~N
```
В открывшемся редакторе:
- `pick` — оставить
- `reword` — оставить, но изменить сообщение
- `squash` — склеить и объединить сообщения
- `fixup` — склеить, игнорируя сообщения маленьких коммитов
- `drop` — удалить коммит

### 4.4. Автосклейка через fixup/Autosquash
```bash
# пометить правку как fixup к старому коммиту
git commit --fixup <hash-целевого-коммита>
# автоматически рассортирует fixup перед ребейзом
git rebase -i --autosquash HEAD~N
```

### 4.5. Редактировать всю историю ветки с первого коммита
```bash
git rebase -i --root
```
> Удобно, когда нужно слить «самые ранние черновики» в initial commit.

### 4.6. Конфликты при ребейзе
```bash
git status
# вручную реши конфликты
# затем
git add <исправленные_файлы>
git rebase --continue
# отменить полностью: git rebase --abort
# пропустить проблемный патч: git rebase --skip
```

### 4.7. Публикация переписанной ветки
```bash
git push --force-with-lease
```
`--force-with-lease` защитит от перетирания чужих пушей.

---

## 🧨 5. Инцидент: секрет или тяжёлые файлы в истории (хирургия)

### 5.1. Резервная копия
```bash
git branch backup/before-rewrite-$(date +%Y%m%d-%H%M)
# или зеркальный бэкап
git clone --mirror . ../backup-repo.mirror
```

### 5.2. Установка инструмента
```bash
brew install git-filter-repo
```

### 5.3. Удаление путей из всей истории
```bash
# удалить конкретный файл из истории
git filter-repo --path .env --invert-paths
# удалить папки/пути целиком
git filter-repo --path node_modules --path .next --invert-paths
# удалить типы файлов
git filter-repo --path-glob '*.zip' --path-glob '*.mp4' --invert-paths
```

### 5.4. Замена секретов по шаблону (оставляя файлы)
```bash
cat > replacements.txt << 'TXT'
password\s*=\s*.+==>password=***
API_KEY\s*[:=]\s*.+==>API_KEY=***
SECRET\s*[:=]\s*.+==>SECRET=***
TXT

git filter-repo --replace-text replacements.txt
```

### 5.5. Упаковка и публикация
```bash
git gc --aggressive --prune=now
git push --force --all
git push --force --tags
```
**Важно:** после утечки ключи/токены **нужно ротировать** в сервисах (Vercel/Render/GitHub/Stripe и т.д.).

---

## 🧱 6. Безопасная «сушка» без переписывания истории
```bash
rm -rf .next .turbo .vercel/cache coverage *.log
pnpm store prune
# уплотнить Git-объекты
git gc --aggressive --prune=now
# ещё глубже
git repack -Ad
```

---

## 🧷 7. Хуки: предотвращаем проблемы заранее

Создай `.git/hooks/pre-commit`:
```bash
#!/usr/bin/env bash

# Блокировка .env
if git diff --cached --name-only | grep -E '^\.env(\.|$)'; then
  echo "❌ Не коммить .env — используй .env.example"; exit 1
fi

# Блокировка файлов > 5MB
big=$(git diff --cached --name-only | xargs -I{} bash -c 'test -f "{}" && [ $(wc -c < "{}") -gt 5242880 ] && echo "{}"')
if [ -n "$big" ]; then
  echo "❌ Слишком большие файлы (>5MB):"; echo "$big"; exit 1
fi
```
Сделай исполняемым:
```bash
chmod +x .git/hooks/pre-commit
```

Опционально — запретить случайные `console.log` в TS/JS перед коммитом (через eslint + lint-staged).

---

## 🔁 8. Политика merge и защита main

- **Squash merge** (рекомендуется для фич): история в main остаётся чистой: «одна фича — один коммит».
- **Rebase & merge**: линейная история, если коммиты уже подчищены.
- **Merge commit**: оставляет ветвления; полезно, если нужны «марки» объединений, но шумнее.

**Branch protection (общая логика):**
1) Включить защиту ветки `main`.
2) Запретить прямые пуши (только через PR).
3) Настроить Required status checks (линт/тесты/билд).
4) Временно разрешать force-push, **только если требуется rewrite**; после — вернуть запрет.

---

## 🤖 9. GitHub Actions: контроль веса и проверок (пример)

`.github/workflows/ci.yml`:
```yaml
name: CI
on: [pull_request]
jobs:
  build-and-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
      - run: corepack enable && pnpm -v
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint && pnpm test --if-present && pnpm build
      - name: Report .git size
        run: |
          du -sh .git || true
          git count-objects -vH || true
```
Можно добавить «фейлить PR», если `size-pack` внезапно вырос сверх порога (скриптом).

---

## 🧭 10. Пошаговый пример (как мы чистили AiNews)

**Задачи:**
- Влить `chore: save before codemod` в конфигурационный коммит.
- Объединить три коммита по husky/hooks в один `chore(ci): setup husky and git hooks`.
- Переименовать `Feature/branch (#1)` → `feat(ci): Configure CI pipeline with automated checks`.

**Действия:**
```bash
# редактируем всю историю, чтобы затронуть ранние коммиты
git rebase -i --root
```
В открывшемся списке переставь/пометь строки так:
```
pick 194375a feat: initial project skeleton setup
fixup 70ce764 architecture pseudocoding

pick a899ad7 build: update some TS/Next/React configurations
fixup 56b0a77 chore: save before codemod

pick 2848439 test: test git hooks setup
fixup baa7eb6 test: test git hooks setup 2
fixup a3de888 fix: fix some husky issues

reword fb8da4a Feature/branch (#1)
pick d22aa51 Testing CI (#2)
```
Когда Git попросит сообщения:
- для объединённого hooks-коммита → `chore(ci): setup husky and git hooks`
- для `fb8da4a` → `feat(ci): Configure CI pipeline with automated checks`

Завершаем и публикуем:
```bash
git log --oneline --graph -10
git push --force-with-lease
```

---

## 🧰 11. Частые ошибки и как их чинить

- `fatal: invalid upstream 'HEAD~N'` — в ветке меньше коммитов, чем N. Используй меньшее N или `--root`.
- Запутался в ребейзе → `git rebase --abort` и начни заново.
- Случайно «сломал» ветку → `git reflog` покажет недавние состояния; вернись: `git reset --hard <sha>`.
- Force push перезаписал чужие коммиты — поэтому всегда используй `--force-with-lease`.

---

## 📚 12. Термины (мини-глоссарий)
- **Rebase** — переписать основание коммитов, изменяя историю.
- **Squash/Fixup** — слить коммиты вместе.
- **Autosquash** — автоматическое упорядочивание fixup/squash-коммитов.
- **Rewrite** — любое изменение истории (rebase, filter-repo и т.п.).
- **GC/Repack** — уплотнение и уборка мусора в объектном хранилище Git.

---

## ✅ 13. Чек-лист перед merge
- История читабельна: 1 фича → 1–3 коммита.
- Нет «final/fix/typo» — они слиты в основной коммит.
- `.env` и тяжёлые файлы не в истории; `.gitignore` строгий.
- CI зелёный; branch protection включён.

---

### ASCII-схема: что делает rebase -i
```
До:
A - B - C - D - E - F - G - H - I (HEAD)

Действия: squash(C+D), fixup(E->B), reword(H)

После:
A - B' - [C+D] - F - G - H' - I
```

> Важно: код не теряется, меняется только «читаемость» истории.

---

**Конец.**


# Git: Ветки и Merge-стратегии

---

## 1. Ветки (branches) — твои параллельные реальности

Git хранит историю коммитов.  
**Ветка = указатель на последовательность коммитов.**

- **main (раньше master)** — главная «линия времени», стабильный код.  
- **feature/** — отдельная ветка, где ты работаешь над новой функцией (например, `feature/login`).  
- **bugfix/** — отдельная ветка для исправления ошибок.  

**Зачем?**  
Чтобы каждый мог работать независимо. Ты создаёшь ветку, пробуешь идеи, ломаешь всё что хочешь — `main` остаётся чистым.

👉 Пример:
```bash
git checkout -b feature/login
git checkout -b feature/show-results (git rule says that you should write feature/)
# тут ты пишешь код логина
```

### Использование git branch

- Создать ветку, но остаться в текущей:
```bash
git branch feature/login
```

- Посмотреть все ветки:
```bash
git branch
```

- Переименовать ветку:
```bash
git branch -m old-name new-name
```

- Удалить ветку:
```bash
git branch -d feature/old
```

Обычно в конспектах показывают `git checkout -b`, так как он сразу создаёт и переключает ветку. Но `git branch` полезен, если нужно отдельно управлять ветками.
# тут ты пишешь код логина
git add .
git commit -m "feat: add login page"
git push origin feature/login

Теперь у тебя есть отдельная ветка для логина.

⸻

2. Merge-стратегии — как «соединять реальности»

Когда ветка готова, её надо «влить» в main. Для этого есть разные способы.

⸻

🟢 Merge commit (обычный merge)

git checkout main
git merge feature/login

Git создаёт дополнительный коммит-слияние.
История будет ветвистой: видно, где была ветка, где её слили.

main:    A---B------E
               \    /
feature:        C--D

	•	✅ Плюс: честная история, видно кто что делал.
	•	❌ Минус: иногда «грязновато» в истории.

⸻

🟡 Squash merge

git checkout main
git merge --squash feature/login

Git берёт все твои коммиты из ветки и схлопывает их в один.

main:    A---B------E
               \
feature:        C--D  (схлопывается в один коммит E)

	•	✅ Плюс: чистая история — в main будет один аккуратный коммит.
	•	❌ Минус: теряется детализация мелких шагов внутри ветки.

Используют, если у тебя в ветке много мелких коммитов типа «fixed typo», «added semicolon».

⸻

🔵 Rebase (переписывание истории)

git checkout feature/login
git rebase main

Git как бы приклеивает твои коммиты поверх последней версии main.
История становится линейной, без «веточек».

До rebase:
main:    A---B
               \
feature:        C--D

После rebase:
main:    A---B---C'---D'

	•	✅ Плюс: красивая линейная история.
	•	❌ Минус: можно сломать общую историю, если делаешь rebase на ветке, которой делятся другие.

⸻

⚪ Fast-forward

Если в main ничего не менялось с момента создания твоей ветки, Git просто «перематывает» указатель.

git merge --ff-only feature/login

До:
main:    A---B
               \
feature:        C--D

После:
main:    A---B---C--D

История как будто ветки и не было.

⸻

3. Как этим реально пользуются команды
	•	Open Source проекты → любят merge commit (видно всё).
	•	Чистые команды стартапов → часто делают squash, чтобы история была аккуратная.
	•	Rebase используют, чтобы подтянуть ветку к последней версии main перед merge (чтобы избежать конфликтов).
	•	Fast-forward — для маленьких правок, которые сразу можно внести в main.

⸻

📌 Пример ситуации
Ты сделал ветку feature/login. Пока писал, твой друг закодил feature/profile и влил её в main.
Чтобы не было конфликтов, ты делаешь:

git checkout feature/login
git pull --rebase origin main

Теперь твои коммиты встанут «после» его, и история будет линейной.

---


⚡ Важный момент: после этого ветка feature/results остаётся локально и на GitHub, но она уже как бы “отработала”. Обычно после проверки, её удаляют, чтобы не плодить мусор:
git branch -d feature/results          # удалить локально
git push origin --delete feature/results  # удалить на GitHub

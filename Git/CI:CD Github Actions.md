# CI/CD — это два связанных понятия, которые часто упоминаются вместе

---

## CI — Continuous Integration (Непрерывная интеграция)
- Разработчики работают над проектом в разных ветках.  
- Каждый пуш или PR запускает автоматические проверки:  
  - линтеры,  
  - тесты,  
  - сборка проекта.  
- Цель: как можно раньше поймать ошибки и убедиться, что код корректно встраивается в общий проект.  

📌 **Пример:**  
Ты пушишь новую фичу в ветку → GitHub Actions запускает линт и тесты → если всё зелёное, значит твой код можно безопасно мёржить в `main`.

---

## CD — Continuous Delivery / Continuous Deployment

Есть два варианта расшифровки:  

1. **Continuous Delivery (Непрерывная доставка)**  
   - После успешных тестов код автоматически готовится к выкладке (собран, упакован).  
   - Но последний шаг — выкладку на продакшн — делает человек вручную (нажимает кнопку “Deploy”).  

2. **Continuous Deployment (Непрерывный деплой)**  
   - Код после успешных тестов и сборки сразу автоматически выкладывается на сервер/хостинг.  
   - Никто вручную не нажимает “Deploy” — всё идёт само.  

---

## Зачем это всё
- ✅ **Скорость** — изменения доставляются быстро, без долгих ручных проверок.  
- ✅ **Качество** — каждая правка проверяется автоматически.  
- ✅ **Безопасность** — защищает `main` от багов и кривого кода.  

---

👉 **По-простому:**  
CI/CD = автоматическая фабрика для кода.  
Ты только пишешь фичу → а система сама проверяет, тестирует и выкатывает её дальше, чтобы ничего не сломалось.  

---

## CI/CD база: Автолинт + Автотесты

### 1. Зачем нужен CI/CD
- **CI (Continuous Integration)** — автоматическая проверка кода при каждом изменении.  
- **CD (Continuous Delivery/Deployment)** — автоматическая доставка кода.  
- В GitHub это реализуется через **GitHub Actions**.  

Идея простая: любой PR проходит автоматическую проверку → только потом попадает в `main`.

---

### 2. Автолинт (Linting)
- Линтер проверяет стиль кода и простые ошибки.  
- Python: `flake8`, JS: `eslint`.  
- При каждом Pull Request GitHub автоматически запускает линтер.  
- Если код не соответствует правилам → PR красный ❌.  
- В `main` попадёт только аккуратный код.  

---

### 3. Автотесты (Testing)
- Тесты проверяют, что программа работает как задумано.  
- Python: `pytest`, JS: `vitest`.  
- В GitHub Actions тесты гоняются на нескольких версиях Python (matrix 3.11–3.13).  
- Если хоть один тест падает → весь PR красный ❌.  
- Пока всё не зелёное ✅ → смержить нельзя.  

---

### 4. Branch Protection
- Настраивается в GitHub → **Settings → Branches**.  
- Включаем:  
  - PR обязателен (нельзя пушить прямо в `main`).  
  - Все проверки (линт + тесты) должны быть пройдены.  

Таким образом, `main` всегда защищён.  

---

### 5. Жизненный цикл работы
1. Разработчик создаёт новую ветку.  
2. Делает изменения и пушит её.  
3. Открывает PR → запускаются Actions.  
4. Линтер + тесты проверяют код.  
5. Если всё прошло ✅ → PR можно смержить.  
6. Если есть ошибки ❌ → исправить → пушнуть снова.  

---

### 6. Итог
- **Автолинт** = единый стиль кода.  
- **Автотесты** = уверенность, что проект работает.  
- **Branch protection** = гарантия, что в `main` всегда только чистый, рабочий код.  
- Всё вместе = автоматическая «система допуска» в продакшн.  